{
  "name": "playground",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "pnpm --dir .. build",
    "build": "rs build",
    "predev": "pnpm --dir .. build",
    "dev": "rs dev"
  }
}
